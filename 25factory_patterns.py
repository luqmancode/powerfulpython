#######################################################################################
# Where the object’s type is fixed, but we want to have several different
# ways to create it. This is called the Simple Factory Pattern.
# Where the factory dynamically chooses one of several different types. This
# is called the Factory Method Pattern.
########################################################################################

